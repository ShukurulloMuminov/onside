DEBUG=True
SECRET_KEY=change-me-in-production
ALLOWED_HOSTS=localhost,127.0.0.1
DATABASE_URL=postgres://onside:onside@localhost:5434/onside
CORS_ALLOWED_ORIGINS=http://localhost:3000
GOOGLE_CLIENT_ID=

# --- Production only (DEBUG=False) ---
# Origins allowed to POST to Django with a Referer/Origin header not
# matching the request host — needed for admin login over HTTPS.
# CSRF_TRUSTED_ORIGINS=https://onside.uz
# Set to false only if TLS is terminated somewhere that doesn't set
# X-Forwarded-Proto (SECURE_SSL_REDIRECT defaults to true otherwise).
# SECURE_SSL_REDIRECT=true
